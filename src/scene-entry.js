/* Entrada del bundle de la escena 3D para el tema de Shopify.
   Sale como asset independiente (dropscents-scene.js) y el tema se lo pasa a
   la interfaz por URL, no por ruta relativa. */
export { createScene } from './scene.js';
